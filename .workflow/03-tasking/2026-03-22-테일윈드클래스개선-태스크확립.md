# 2026-03-22-테일윈드클래스개선-태스크확립

## 체크리스트
- [ ] `src/app/pages/block.vue` 파일 수정
  - `font-semibold` -> `font-600`
  - `font-bold` -> `font-700`
  - `text-base` -> `text-sm`
- [ ] 전체 `.vue` 파일 재검색 (누락된 `text-base`, `rounded-md`, `rounded-lg` 등 확인)
- [ ] 검증 (Grep 검색 결과 0건 확인)

## 태스크 상세
### Task 1: `block.vue` 개선
- 목표: 무효화된 클래스 교체
- 완료 조건: 파일 내 해당 문자열이 테마 클래스로 대체됨.

### Task 2: 전수 조사 및 검증
- 목표: 모든 `.vue` 파일에서 무효화된 클래스가 남아있는지 확인.
- 완료 조건: `text-base`, `rounded-md`, `rounded-lg` 등이 발견되지 않음.

---
💡 **상태**: 3단계: 태스크 확립 완료
📍 **현재 작업**: 작업 체크리스트 수립 완료
⏭️ **다음 작업**: 4단계: 수행 진행
