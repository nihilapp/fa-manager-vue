# 체크리스트 (Checklist)

## 요약
- 설계 기반: `.workflow/02-design/2026-03-30-플레이어아이디라우트정상화-설계.md`
- 총 태스크 수: 2
- 현재 활성 태스크: 없음

## 작업 목록
- [x] Task 1: `[id]` 세 라우트를 대상 플레이어 관리 로직으로 복구
목표: `GET`, `PUT`, `DELETE`가 URL 파라미터의 플레이어 ID를 기준으로 동작하도록 수정한다.
완료 조건: 세 라우트가 `getRouterParam(event, 'id')`를 사용하고 더 이상 `user!.id`를 대상 식별자로 쓰지 않는다.
의존성: `.workflow/02-design/2026-03-30-플레이어아이디라우트정상화-설계.md`

- [x] Task 2: 어드민 전용 제약과 타입 검증 완료
목표: `[id]` 라우트에 어드민 전용 제약을 추가하고 정적 검증을 통과시킨다.
완료 조건: 세 라우트 모두 `isAdmin` 검사를 수행하고 `pnpm.cmd exec tsc --noEmit`를 통과한다.
의존성: Task 1

## 진행 로그
- Task 1 완료, 다음 작업으로 이동
- Task 2 완료, 다음 작업으로 이동
