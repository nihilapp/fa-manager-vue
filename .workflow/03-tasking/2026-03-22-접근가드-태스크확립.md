# 체크리스트 (Checklist)

## 요약
- 설계 기반: `2026-03-22-접근가드-설계.md`
- 총 태스크 수: 4
- 현재 활성 태스크: 없음

## 작업 목록
- [x] Task 1: 공통 접근 체크 유틸 정비
  - 목표: `X-Discord-ID`, 개발 환경 여부, 유저 존재 여부를 한 군데에서 판정하는 서버 유틸을 만든다.
  - 완료 조건: `src/server/utils/access.ts`가 생성되고 `users/check`와 서버 미들웨어가 같은 검증 로직을 재사용해야 한다.
  - 의존성: `2026-03-22-접근가드-설계.md`

- [x] Task 2: 서버 미들웨어 기반 전역 접근 가드 구현
  - 목표: 페이지 렌더링 전에 접근 가능 여부를 판정하고 실패 시 `/block`으로 보내는 Nitro 서버 미들웨어를 구현한다.
  - 완료 조건: `src/server/middleware/access.ts`가 생성되고, `/block` 및 정적 자산 예외 처리와 실패 시 리다이렉트 로직이 포함되어 있어야 한다.
  - 의존성: Task 1 완료

- [x] Task 3: 차단 페이지 및 check 엔드포인트 정비
  - 목표: 차단 대상의 종착 페이지를 만들고 `users/check` 엔드포인트를 공통 검증 유틸 기반으로 정리한다.
  - 완료 조건: `src/app/pages/block.vue`가 생성되어 차단 메시지를 표시하고, `src/server/api/users/check.get.ts`가 공통 유틸을 사용해야 한다.
  - 의존성: Task 2 완료

- [x] Task 4: 동작 검증 및 문서 갱신 준비
  - 목표: 설계 대비 구현 누락 여부와 기본 동작 시나리오를 점검하고, 다음 검증 단계 문서 작성에 필요한 근거를 정리한다.
  - 완료 조건: 정상 접근, 차단 접근, `block` 예외 처리, 정적 자산 우회 여부를 기준으로 점검 결과가 정리되어 있어야 한다.
  - 의존성: Task 3 완료

## 진행 로그
- 현재 시작 상태: Task 1 활성화
- Task 1 완료, 다음 작업으로 이동
- Task 2 완료, 다음 작업으로 이동
- Task 3 완료, 다음 작업으로 이동
- Task 4 완료, 다음 작업으로 이동
