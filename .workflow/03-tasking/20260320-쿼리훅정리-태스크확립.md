# 20260320-쿼리훅정리-태스크확립

## 1. 태스크 체크리스트
- [x] Task 1. `api-error-handler.ts`의 미사용 API 제거
  - 목표: 외부 공개 API를 실제 사용되는 범위로 축소합니다.
  - 완료 조건: 토스트 관련 상태와 `throwIfError`가 제거되고 `checkAndHandleApiError`만 유지됩니다.
  - 의존성: 없음

- [x] Task 2. `useApiRequest.ts` 공통 처리 최소화
  - 목표: query/mutation 훅이 공통으로 사용할 응답/에러 처리 함수만 남깁니다.
  - 완료 조건: `handleApiResponse`, `handleApiRequestError`가 정의되고 기존 `createApiMutation`이 제거됩니다.
  - 의존성: Task 1

- [x] Task 3. `useGet` 객체 인자 구조 전환
  - 목표: `useGet`을 객체 인자 기반으로 전환하고 반환 형태를 `response`, `execute` 중심으로 통일합니다.
  - 완료 조건: 위치 기반 인자가 제거되고 `user.store.ts`가 새 형식으로 갱신됩니다.
  - 의존성: Task 2

- [x] Task 4. mutation 훅 직접 `useMutation` 호출로 전환
  - 목표: `usePost`, `usePut`, `usePatch`, `useDelete`가 동일한 객체 인자 구조와 반환 형태를 갖습니다.
  - 완료 조건: 네 파일 모두 내부에서 직접 `useMutation`을 호출합니다.
  - 의존성: Task 2

- [x] Task 5. 타입 검증
  - 목표: 변경 후 타입 오류가 없는지 확인합니다.
  - 완료 조건: `pnpm.cmd exec tsc --noEmit`가 성공합니다.
  - 의존성: Task 3, Task 4

## 2. 진행 로그
- Task 1 완료, 다음 작업으로 이동
- Task 2 완료, 다음 작업으로 이동
- Task 3 완료, 다음 작업으로 이동
- Task 4 완료, 다음 작업으로 이동
- Task 5 완료, 다음 작업으로 이동
