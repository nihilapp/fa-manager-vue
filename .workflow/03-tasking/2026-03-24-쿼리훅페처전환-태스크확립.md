# 체크리스트 (Checklist)

## 요약
- 설계 기반: `.workflow/02-design/2026-03-24-쿼리훅페처전환-설계.md`
- 총 태스크 수: 3
- 현재 활성 태스크: Task 1

## 작업 목록
- [x] Task 1: 공통 API 요청 유틸과 `useGet`을 `useFetch` 기반으로 전환
- 목표:
  - GET 요청이 기본 수동 실행으로 동작하는 호환 레이어를 만든다.
- 완료 조건:
  - `useGet.ts`가 Vue Query 없이 동작한다.
  - 기본 자동 실행이 꺼져 있고 `execute()`로 요청할 수 있다.
  - 공통 에러 처리 유틸이 Fetch 에러를 처리할 수 있다.
- 의존성:
  - `.workflow/01-analysis/2026-03-24-쿼리훅페처전환-분석.md`
  - `.workflow/02-design/2026-03-24-쿼리훅페처전환-설계.md`

- [x] Task 2: mutation 계열 훅을 `$fetch` 기반으로 전환
- 목표:
  - `usePost`, `usePut`, `usePatch`, `useDelete`의 내부를 Vue Query 없이 수동 실행 상태 모델로 맞춘다.
- 완료 조건:
  - 네 훅 모두 `execute()`와 `response`를 유지한다.
  - `pending`, `status`, `error` 상태가 로컬 refs로 제공된다.
- 의존성:
  - Task 1

- [x] Task 3: 실제 호출부 및 설정 잔존물 정리
- 목표:
  - `user.store.ts` 호출부와 Nuxt/Vue Query 잔존 설정을 정리한다.
- 완료 조건:
  - `user.store.ts`가 `execute()`를 명시 호출한다.
  - `nuxt.config.ts`의 Vue Query auto-import/optimizeDeps가 제거된다.
  - `src/app/plugins/vue-query.ts`가 제거된다.
- 의존성:
  - Task 1
  - Task 2

## 진행 로그
- Task 1 완료, 다음 작업으로 이동
- Task 2 완료, 다음 작업으로 이동
- Task 3 완료, 다음 작업으로 이동
