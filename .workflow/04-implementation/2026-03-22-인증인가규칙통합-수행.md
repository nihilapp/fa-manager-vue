# 구현 메모 (Implementation Memo)
- 활성 태스크: Task 1
- 수정 파일:
  - `src/server/utils/auth.ts`
  - `src/server/utils/access.ts`
  - `src/server/api/sessions/[id].delete.ts`
  - `.workflow/04-implementation/2026-03-22-인증인가규칙통합-수행.md`
- 간단 메모:
  - `authHelper` 반환 계약을 운영/개발 공통 컨텍스트 형태로 고정
  - 개발 환경에서는 첫 관리자 우선, 없으면 첫 유저를 fallback user로 조회하는 우회 정책 적용
  - 에러 상황에서도 동일한 필드 구조를 유지해 후속 태스크에서 호출부 보강이 가능하도록 정리
  - 임시 검증으로 `pnpm.cmd exec eslint src/server/utils/auth.ts` 실행 성공
  - 페이지 접근 유틸이 독립 규칙을 가지지 않고 `authHelper` 결과만 소비하도록 정리
  - 임시 검증으로 `pnpm.cmd exec eslint src/server/utils/access.ts src/server/middleware/access.ts src/server/api/users/check.get.ts` 실행 성공
  - `sessions/[id].delete.ts`의 직접 헤더 검사 제거로 예외 인증 분기를 공통 헬퍼 기준으로 통일
  - 임시 검증으로 `pnpm.cmd exec eslint "src/server/api/sessions/[id].delete.ts"` 실행 성공
  - 개발 환경에서 헤더 유저 조회 실패 시에도 fallback user를 재시도하고, 최종적으로 사용자를 찾지 못하면 에러를 반환해 CUD API의 `user!.id` null 참조를 차단
  - 임시 검증으로 `pnpm.cmd exec eslint src/server/utils/auth.ts "src/server/api/campaigns/index.post.ts" "src/server/api/characters/index.post.ts" "src/server/api/sessions/index.post.ts" "src/server/api/users/[id].put.ts"` 실행 성공
