# 구현 메모 (Implementation Memo)
- 활성 태스크: 완료
- 수정 파일:
  - `src/app/components/common/FormSelect.vue`
- 간단 메모:
  - `FormSelect`는 옵션 배열을 읽기만 하므로 `options` 프롭에 readonly 배열 허용 타입을 적용한다.
  - 호출부 `NewPlayerForm.vue`는 수정하지 않고 기존 `as const` 상수를 그대로 유지하는 방향으로 구현한다.
  - 실제 수정은 `options?: readonly SelectOption[]` 1건으로 제한해 부작용 범위를 최소화했다.
