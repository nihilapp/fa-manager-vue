# 20260320-수동앱임포트타입정비-수행

## 참조 문서
- `.workflow/03-tasking/20260320-수동앱임포트타입정비-태스크확립.md`

## 활성 태스크
- 완료된 태스크 없음

## 구현 메모
- 수정 파일: `src/app/types/manual-app-imports.d.ts`
- 판단 근거: `#imports` 집합 인덱싱 대신 실제 공개 모듈 타입을 직접 참조해야 편집기에서 전역 심볼 시그니처가 보존됩니다.
- 임시 검증 결과: `tsc --noEmit -p tsconfig.json` 통과, `pnpm exec nuxi typecheck`는 로컬 `vue-tsc` 부재로 레지스트리 접근 단계에서 실패
- 현재 상태: 구현 및 대체 검증 완료
