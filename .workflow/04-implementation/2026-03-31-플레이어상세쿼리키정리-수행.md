# 구현 메모 (Implementation Memo)

- 활성 태스크: Task 3
- 수정 파일:
  - `src/app/composables/players/useGetPlayerInfo.ts`
  - `src/app/composables/players/useGetPlayerSessionList.ts`
- 간단 메모:
  - 상세 조회 훅의 키를 `players.detail({ id })`로 수정
  - `useGetPlayerSessionList`를 `queryKeys.players.sessions({ playerId, ...query })` 기준으로 완성
  - 에러 콜백 타입을 `ApiErrorResponse`로 통일
  - 타입 검증은 `pnpm.cmd exec tsc --noEmit`로 수행
