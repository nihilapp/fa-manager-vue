# 구현 메모 (Implementation Memo)
- 활성 태스크: Task 2
- 수정 파일:
  - `src/app/stores/character.store.ts`
- 간단 메모:
  - `campaignId` 검증을 nullable 정수 전처리 기반으로 전환
  - `startLevel`, 시작 자금 필드를 문자열 정규화 후 정수 검증으로 전환
  - `z.union(...)` 제거로 `@vee-validate/zod`의 `invalid_union` 런타임 예외 경로 우회
