# 구현 메모 (Implementation Memo)
- 활성 태스크:
  - Task 1: 기본 레이아웃 컨테이너 높이 제약 보강
  - Task 2: `main` 전용 스크롤 컨테이너 적용
- 수정 파일:
  - `src/app/layouts/default.vue`
  - `src/app/components/layout/common-layout/CommonContent.vue`
- 간단 메모:
  - `#__nuxt`의 `100dvh`/`overflow: hidden` 정책은 유지
  - 기본 레이아웃 래퍼와 `main`에 `min-h-0`을 추가해 flex 문맥에서 축소를 허용
  - `main`에 `overflow-y-auto`를 부여해 본문만 스크롤되도록 처리
