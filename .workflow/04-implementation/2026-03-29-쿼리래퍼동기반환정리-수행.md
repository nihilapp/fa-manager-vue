# 구현 메모 (Implementation Memo)
- 활성 태스크: Task 3 완료
- 수정 파일:
  - `src/app/composables/query/useGet.ts`
  - `src/app/composables/players/useCreateNewPlayer.ts`
- 간단 메모:
  - `useGet`의 top-level `async`를 제거해 composable 자체는 동기 반환이 되도록 정리했다.
  - `refetch`는 `return execute()`로 단순화했다.
  - `useCreateNewPlayer`는 `usePost` 반환 객체를 그대로 반환하면서 성공 콜백도 실제 데이터 `T`를 받도록 맞췄다.
- 임시 검증 결과:
  - `pnpm.cmd exec tsc --noEmit --pretty false` 성공
