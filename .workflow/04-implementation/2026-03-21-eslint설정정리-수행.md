# 구현 메모 (Implementation Memo)
- 활성 태스크: Task 1 -> Task 2 -> Task 3
- 수정 파일:
  - `eslint.config.mjs`
  - `.workflow/03-tasking/2026-03-21-eslint설정정리-태스크확립.md`
- 간단 메모:
  - `sharedTsRules`를 추가해 `.ts`와 `.vue`가 동일한 TypeScript 규칙 세트를 공유하도록 정리
  - import-x 관련 블록과 Vue SFC 전용 블록을 분리해 `.ts` 파일이 Vue 파서 블록에 포함되지 않도록 재구성
  - JS/TS 공통 규칙 블록의 `files` 범위를 `**/*.{js,ts,vue}`로 확장해 `.vue`도 기본 공통 규칙을 동일하게 적용받도록 조정
  - `pnpm.cmd exec eslint eslint.config.mjs src/app/pages/index.vue src/app/plugins/vue-query.ts` 검증 통과
