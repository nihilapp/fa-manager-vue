# 수행 (Implementation)

## 요약
- 참조 태스크 문서: `.workflow/03-tasking/2026-04-01-플레이어목록렌더오류수정-태스크확립.md`
- 현재 활성 태스크: 완료
- 수정 파일:
  - `src/app/stores/player.store.ts`
  - `src/app/components/players/PlayerListTable.vue`

## 구현 메모
- `player.store.ts`에 `getPlayerStatusInfo` 헬퍼와 fallback 상태 정보(`알 수 없음`, `gray`)를 추가했다.
- `PlayerListTable.vue`는 상태 슬롯에서 `playerStatusMap.get(... )!` 직접 접근을 제거하고 공통 헬퍼를 사용하도록 변경했다.
- 보고된 목록 페이지 오류 범위에 맞춰 상세 화면 변경은 커밋 대상에서 제외했다.

## 임시 검증 결과
- `pnpm.cmd exec eslint src/app/stores/player.store.ts src/app/components/players/PlayerListTable.vue` 통과
- `pnpm.cmd exec nuxi typecheck`는 `https://registry.npmjs.org/vue-tsc` 접근 실패(EACCES)로 완료하지 못함
